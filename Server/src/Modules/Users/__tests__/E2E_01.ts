/**
 * -- E2E MODULE OUTLINE (1)--
 * Try to login (valid)
 * Try to grab token from login response
 * Try to edit user (valid)
 * Try to logout (valid)
 * Try to login (valid)
 * Try to query users (valid, should fail)
 * Try to query user (self, valid)
 * Try to delete self (valid)
 * Try to logout
 */
describe('User Module: End2End Test - 01', () => {
    it('Should not doing anything at the moment', () => {
        expect(1).toBe(1);
    })
})